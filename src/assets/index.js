import ASSETS from './assets';
import {
  COLOR,
  FONT,
  screenHeight,
  screenWidth,
  windowHeight,
  windowWidth,
  navigation,
} from './assets';

export {
  ASSETS,
  COLOR,
  FONT,
  screenHeight,
  screenWidth,
  windowHeight,
  windowWidth,
  navigation,
};
